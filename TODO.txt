TODO - Projeto Promoções (Event-Driven, RabbitMQ)

1. Explorar código existente (concluído)

2. Gerar chaves assimétricas para componentes
   - gateway (private.pem + public)
   - ms-promocao (private.pem + public)
   - ms-ranking (private.pem + public)
   - ms-notificacao (private.pem + public)
   - clientes/lojas (private.pem + public)

3. Definir esquema de eventos e routing keys
   - comando.promocao.registrar
   - comando.promocao.listar
   - comando.ranking.pontuar
   - retorno.* (respostas síncronas ao gateway)
   - evento.promocao.criada.<categoria>
   - evento.alerta.hot.<categoria>
   - evento.alerta.enviar.<categoria>

4. Implementar MS Gateway - endpoints REST
   - POST /promocoes (loja)
   - GET /promocoes
   - POST /promocoes/{id}/vote
   - POST /subscribe (registrar interesses)
   - POST /unsubscribe (opcional)
   - Validar assinaturas das mensagens recebidas da loja

5. Implementar MS Gateway - SSE e gerenciamento de inscrições
   - Endpoint SSE: /sse/{client_id}
   - Roteamento de notificações por categoria
   - Keep-alive e reconexão

6. Implementar verificação de assinatura no MS Promoção
   - Validar assinatura digital das mensagens recebidas (usar public keys)
   - Rejeitar promoções sem assinatura válida

7. Implementar MS Promoção - persistência e publicação
   - Persistir promoções recebidas (db.json)
   - Publicar evento promocao.publicada / evento.promocao.criada.<categoria>
   - Responder ao gateway com retorno.promocao.registro

8. Implementar MS Ranking - processar votos e identificar hot deals
   - Consumir comando.ranking.pontuar
   - Atualizar score por promocao
   - Quando atingir limite (p.ex. >= 5 votos), publicar evento.alerta.hot.<categoria>
   - Publicar retorno.ranking.pontuacao para o gateway

9. Implementar MS Notificação - consumir eventos e validar assinaturas
   - Consumir promocao.publicada e promocao.destaque/hot
   - Validar assinaturas
   - Decidir notificações por loja e por categoria
   - Publicar evento.evento.alerta.enviar.<categoria> destinado ao gateway
   - Integrar com API externa para enviar e-mails (Resend ou similar) para lojas

10. Integrar API externa de e-mail (ex: Resend)
    - Implementar cliente HTTP para enviar e-mails (templates simples)
    - Configurar variáveis de ambiente para API key
    - Registrar logs de envio e tentativa/erro

11. Implementar cliente Loja (assinar e enviar promoções)
    - Gerar chave privada da loja
    - Assinar payloads antes de enviar via gateway
    - Testar workflows de registro de promoção

12. Desenvolver frontend (linguagem distinta) com REST + SSE
    - Página para consumidores: listar promoções, votar, seguir categorias
    - Registrar interesse por categoria no backend via REST
    - Conectar via SSE para receber notificações em tempo real

13. Atualizar docker-compose para orquestração dos serviços
    - Serviços: rabbitmq, gateway, ms-promocao, ms-ranking, ms-notificacao, clientes, frontend
    - Volumes para chaves e DBs locais

14. Adicionar testes unitários e E2E para fluxos principais
    - Testar publicação/consumo de eventos
    - Testar validação de assinaturas
    - Testar SSE e reconexão no frontend

15. Documentar APIs, eventos e como executar o sistema
    - README com passos de setup e execução (Docker / virtualenv)
    - Especificação dos endpoints REST e eventos RabbitMQ

16. Testes de aceitação manuais e ajustes finais
    - Testar cenário completo: loja registra promoção -> ms-promocao registra -> consumidores recebem SSE; votos -> ms-ranking publica hot deal -> ms-notificacao envia e-mail

Notas e prioridades:
- Prioridade inicial: gerar chaves, gateway REST+SSE, ms-promocao e ms-ranking básicos.
- Definir limite de votos para hot deal (ex: 5) e documentar.


Generated by assistant.